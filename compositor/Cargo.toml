[package]
name = "rustos-compositor"
description = "Renderer-neutral scene, focus and frame-clock policy for ring-3 compositord"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
rustos-abi = { path = "../abi" }
rustos-video = { path = "../video" }

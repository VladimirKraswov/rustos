# Артефакты сборки
/target/
/build/

# Сгенерированные payload-файлы загрузчика (ядро + initramfs, встраиваются
# через include_bytes! в момент сборки загрузчика)
/boot/uefi/payload/

# Локальные образы и логи
*.img
*.log
.DS_Store

# Локальный журнал рабочего агента, не часть исходного дерева ОС.
/.opencode/

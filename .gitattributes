# Системные raster assets являются бинарными даже если конкретный RGB565
# кадр случайно не содержит NUL в начале файла. Так Git не строит бессмысленный
# line diff и не пытается нормализовать байты на другой платформе.
*.png binary
*.rgb565 binary
